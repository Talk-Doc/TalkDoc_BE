spring:
  application:
    name: talkdoc-backend
  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 55MB
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

server:
  port: ${SERVER_PORT:8080}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health,info

talkdoc:
  session:
    ttl: 2h
  sign:
    confidence-threshold: 0.75
  ai:
    provider: ${TALKDOC_AI_PROVIDER:mock}      # mock | gemini
    timeout: ${TALKDOC_AI_TIMEOUT:60s}   # STT(오디오 inline)·LLM 응답이 15초를 넘길 수 있음
    gemini:
      api-key: ${GEMINI_API_KEY:}
      base-url: https://generativelanguage.googleapis.com/v1beta
      thinking-level: ${GEMINI_THINKING_LEVEL:low}   # Gemini 3.x thinking 억제 (기본값이면 호출당 20~30초)
      models:
        stt: ${GEMINI_STT_MODEL:gemini-3.7-flash}   # lite 는 STT 정확도가 떨어져 STT 만 상위 모델
        llm: ${GEMINI_LLM_MODEL:gemini-3.5-flash-lite}
        tts: ${GEMINI_TTS_MODEL:gemini-2.5-flash-preview-tts}
  sign-ai:
    mode: ${TALKDOC_SIGN_AI_MODE:mock}         # mock | http
    base-url: ${TALKDOC_SIGN_AI_URL:http://localhost:8000}
    timeout: 10s
  websocket:
    allowed-origins: ${TALKDOC_WS_ORIGINS:*}
